import Slider from "react-slick";
import styled from "styled-components";

const StyledSlider = styled(Slider)``;

const StyledBook = styled.div`
  padding: 10px 15px;
`;

export { StyledSlider, StyledBook };
