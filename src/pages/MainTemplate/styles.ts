import styled from "styled-components";
import { Breakpoint } from "../../ui/breakpoints";

export const StyledMainTemplate = styled.div`
    max-width: ${Breakpoint.XS}px;
    padding: 0 20px;
    margin: 0 auto;
`