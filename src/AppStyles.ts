import styled from "styled-components";
import { media } from "./ui/media";

export const StyledApp = styled.div`
    height: 100vh;
    width: 1300px;
    ${media.TABLET} {

    }

    ${media.PHONE} {
        
    }
`;